sudo apt-get install libmariadb3 libmariadb-dev #need installed for pip install mariadb
sudo apt install pipenv
pip install virtualenv==20.7.2  #specific version works with pipenv

ansible-playbook -u jenkins env-playbook.yml -v
pip install docker
pip install -- update wheel
pip install -- update setuptools
pip install -r requirements.txt
docker build -t marcuskielman/devops_api .
docker images ls -a
                    