Commands for setting up MariaDB Container:

docker run -p 3306:3306 --name db --network maria_db -v data:/data -e MYSQL_DATABASE=classicmodels -e MYSQL_ROOT_PASSWORD=root -d mariadb
docker run -p 3306:3306 -h mariadb --name=mariadb --network maria_db -d marcuskielman/mariadb -->runs on host mariadb
mariadb -h 127.0.0.1 -u root -p
source ./mysqlsampledatabase.sql
docker network create maria_db

Commands for Setting up devops_api Container:
docker build -t marcuskielman/devops_api .
docker images ls -a

Commands for marcuskielman/mariadb Container:
docker pull marcuskielman/mariadb
docker run -p 3306:3306 -h mariadb --network api_maria --name maria_db  -d marcuskielman/mariadb
docker exec -i mariadb mysql -uroot -proot classicmodels < mysqlsampledatabase.sql